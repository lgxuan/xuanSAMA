import java.util.Random;

public class SameNum {
    //目标：从一个包含1-1000的数组中找出一个出现了两次的数
    //不用辅助储存空间，每个数据只能遍历一次
    public static void main(String[] args) {
        int test = new Random().nextInt(10);
        System.out.println(test);
    }
}
